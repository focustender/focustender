# Resume

Drop your resume as a `.pdf` anywhere in this folder — any filename
works (e.g. `resume.pdf`, `ian-castorillo-resume.pdf`) — and the link on
the Work page picks it up automatically on the next build, no code
changes needed.

If more than one `.pdf` ends up in here, the first one alphabetically is
used, so keep it to one file at a time.
